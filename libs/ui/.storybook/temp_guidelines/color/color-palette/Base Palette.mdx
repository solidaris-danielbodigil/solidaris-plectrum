import { Meta, Story } from '@storybook/addon-docs';
import * as ColorStories from './colors.component.stories';

<Meta of={ColorStories} />

# Base Palette

> **Base and cta colors are used inside a color mix in order to generate Semantic colors <br/>**
> **Use only Semantic colors inside your classes**

<Story of={ColorStories.Base} />

