import { Meta, Story, Unstyled } from '@storybook/addon-docs/blocks';
import { DocsTable } from '../../.storybook/docs-table';
import * as Stories from './typography.stories';

<Meta title="Foundations/Typography/Roles" />

# Typography roles

## Choose a hierarchy

One page, top to bottom:

1. **Display** or **heading lg** — the page title, once.
2. **Heading md** — a section.
3. **Heading sm** — a card or a group inside that section.
4. **Body** — sentences and values.
5. **Label** — control names and table headers. Do not set a whole paragraph in a label role.

Do not skip from the page title to a tiny label with nothing in between. On a narrow screen, keep the same roles — do not invent a smaller size. Long French labels belong in the body or label role you already chose; check them in the playground below.

Type styles live in code, not as Figma variables. Base is `1rem = 14px`. Each role bundles family, size, weight, line-height and letter-spacing.

Type your own copy in the playground field to check a real string — long French labels and currency amounts behave differently from lorem ipsum.

## Usage

<Unstyled>
  <Story of={Stories.Usage} />
</Unstyled>

## Examples

```scss
// 06-components/_components.card.scss
@use '../02-tools/tools.typography' as *;

.c-card__title { @include text-heading-sm; }
.c-card__meta  { @include text-body-xs; }
```

## How to use

1. In `06-components`, include a mixin: `@use "../02-tools/tools.typography" as *; .c-x__title { @include text-heading-sm; }`.
2. Use a raw `--pds-text-{role}-{size}-{prop}` var only when you need one property in isolation.
3. Take the whole role. Mixing a size from one role with a line-height from another is how vertical rhythm drifts.

Copy gives you `@include text-body-md;` for component SCSS, `.u-text-body-md` for a template, or the resolved `font` shorthand.

Declared in `_settings.typography-primitive.scss` and `_settings.typography-semantic.scss`. Classes: `.u-text-{role}-{size}`. Primitives live on [Foundations / Typography / Primitives](?path=/docs/foundations-typography-primitives--docs).

## Roles

<DocsTable
  headers={['Role', 'When']}
  rows={[
    ['Display', 'Hero metrics and page-level figures. Agenda.'],
    ['Heading', 'Section and card titles. Open Sans, bold.'],
    ['Label', 'Form labels, table headers, badges, UI chrome.'],
    ['Body', 'Running copy and descriptions.'],
  ]}
/>

<Unstyled>
  <Story of={Stories.Roles} />
</Unstyled>
