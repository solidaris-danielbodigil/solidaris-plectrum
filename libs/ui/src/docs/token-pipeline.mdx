import { Meta, Story, Unstyled } from '@storybook/addon-docs/blocks';
import { DocsTable } from '../../.storybook/docs-table';
import { Diagram } from '../../.storybook/docs-figures';
import * as Figures from './token-pipeline.stories';

<Meta title="Docs/Token pipeline" />

# Token pipeline

The token pipeline moves design tokens between the Plectrum UI Kit in Figma, this design-system repository, and the applications that install it.

- **Figma** holds the design decisions (colours, radii, shadows, transitions, focus).
- **The repository** imports them, generates CSS custom properties, documents them in this Storybook, and publishes versioned packages.
- **Applications** (iSHARE, iCRM) install a package version and reference tokens as `var(--pds-*)`.

Drift between Figma and CSS fails CI. No automated step writes the main Figma file.

Related pages:

- [Figma sync](?path=/docs/docs-token-pipeline-figma-sync--docs) — Figma → repo import, repo → Figma proposals, package distribution
- [CSS-first surface](?path=/docs/docs-token-pipeline-css-first-surface--docs) — the CSS contract for component authors
- [Writing stories](?path=/docs/docs-writing-stories--docs) — CSF + attached MDX for designers and developers
- [AI strategy](?path=/docs/docs-ai-strategy--docs) — Contract-Driven Development: index, metadata, protocols
- [CSS architecture](?path=/docs/docs-css-architecture--docs) — ITCSS layers and BEMIT mixes
- **Foundations** — token browsers, rendered from the compiled CSS by `<pds-token-explorer>`

---

## Definition

A token is a named design value. The name is stable across tools; the value is defined once, in Figma.

<Diagram
  title="Token lifecycle"
  width={1000}
  height={150}
  nodes={[
    { id: 'figma', x: 20, y: 43, w: 170, tone: 'design', title: 'Figma variable', lines: ['primary/600 = #487395'], mono: true },
    { id: 'json', x: 275, y: 43, w: 160, tone: 'system', title: 'tokens.json', lines: ['primary.600'], mono: true },
    { id: 'css', x: 520, y: 43, w: 210, tone: 'system', title: 'CSS custom property', lines: ['--pds-color-primary-600'], mono: true },
    { id: 'ui', x: 815, y: 43, w: 165, tone: 'app', title: 'Component', lines: ['var(--pds-color-…)'], mono: true },
  ]}
  edges={[
    { from: 'figma', to: 'json', label: 'plugin sync' },
    { from: 'json', to: 'css', label: 'tokens:build' },
    { from: 'css', to: 'ui', label: 'var()' },
  ]}
/>

<DocsTable
  headers={['Stage', 'Name', 'Owner']}
  rows={[
    ['Figma variable', 'primary/600', 'Designer'],
    ['tokens.json', 'primary.600', 'Repository — imported, never edited by hand'],
    ['CSS custom property', '--pds-color-primary-600', 'Repository — generated by tokens:build'],
    ['Component style', 'var(--pds-color-primary-600)', 'Developer'],
  ]}
/>

Diagram legend, used on all pipeline pages: **orange** = Figma, **blue** = design-system repository, **green** = application, grey = process. Dashed arrows are manual steps.

---

## Architecture

<Diagram
  title="Pipeline architecture"
  width={960}
  height={430}
  lanes={[
    { title: 'Figma', subtitle: 'designers', x: 0, w: 305, tone: 'design' },
    { title: 'Design-system repository', subtitle: 'Plectrum · Storybook', x: 327, w: 305, tone: 'system' },
    { title: 'Applications', subtitle: 'iSHARE · iCRM', x: 655, w: 305, tone: 'app' },
  ]}
  nodes={[
    { id: 'kit', x: 42, y: 80, w: 220, h: 76, tone: 'design', title: 'Plectrum UI Kit', lines: ['main Figma file', 'Primitive · Semantic · Component'] },
    { id: 'branch', x: 42, y: 320, w: 220, h: 76, tone: 'design', title: 'Figma branch', lines: ['proposals/{app}', 'reviewed and merged by a designer'] },
    { id: 'json', x: 369, y: 80, w: 220, h: 76, tone: 'system', title: 'tokens.json', lines: ['imported Figma variables', 'ingestion source of truth'] },
    { id: 'css', x: 369, y: 200, w: 220, h: 76, tone: 'system', title: 'Generated CSS + Storybook', lines: ['--pds-* custom properties', 'Foundations pages'] },
    { id: 'code', x: 369, y: 320, w: 220, h: 76, tone: 'system', title: 'Code-owned tokens', lines: ['--pds-* declared in 01-settings', 'spacing, typography, features'] },
    { id: 'pkg', x: 697, y: 200, w: 220, h: 76, tone: 'app', title: 'Published packages', lines: ['@solidaris/ui · styles · plectrum', 'versioned together'] },
    { id: 'apps', x: 697, y: 320, w: 220, h: 76, tone: 'app', title: 'iSHARE · iCRM', lines: ['install published versions'] },
  ]}
  edges={[
    { from: 'kit', to: 'json', label: '1 · plugin sync' },
    { from: 'json', to: 'css', label: '2 · build' },
    { from: 'css', to: 'pkg', label: '3 · publish' },
    { from: 'pkg', to: 'apps', label: '4 · install' },
    { from: 'code', to: 'css', label: 'compiled' },
    { from: 'code', to: 'branch', label: '5 · propose' },
    { from: 'branch', to: 'kit', label: '6 · merge + publish', dashed: true },
  ]}
/>

<DocsTable
  headers={['#', 'Transport', 'Mechanism']}
  rows={[
    ['1', 'Figma → repository', 'PrimeUI theme generator plugin, GitHub sync to the staging branch design-tokens/sync. A workflow promotes staging to tokens.json through a pull request.'],
    ['2', 'tokens.json → CSS', 'tokens:build (Style Dictionary) writes *.generated.scss and the Storybook manifest tokens.generated.ts.'],
    ['3', 'Repository → packages', 'Changesets version and publish @solidaris/ui, @solidaris/styles and @solidaris/plectrum together.'],
    ['4', 'Packages → applications', 'Renovate or Dependabot opens a bump pull request in each application repository.'],
    ['5', 'Code → Figma branch', 'tokens:propose lists code-owned tokens missing from tokens.json; tokens:apply writes them to the Figma branch proposals/{app}.'],
    ['6', 'Figma branch → main file', 'A designer reviews, merges and publishes the library. The LIBRARY_PUBLISH event triggers a re-pull.'],
  ]}
/>

Constraints:

- Automated writes target Figma branches only. The main Figma file is edited and published by designers.
- Applications install a package version. Token values are never copied into an application repository.
- Code-owned tokens (spacing, typography, feature-specific values) compile into the same stylesheet as imported tokens.

---

## Change flow

Sequence for one token change, from Figma to a running application.

<Unstyled>
  <Story of={Figures.ChangeFlow} />
</Unstyled>

---

## Roles

<Unstyled>
  <Story of={Figures.Roles} />
</Unstyled>

---

## Rules

<Unstyled>
  <Story of={Figures.Rules} />
</Unstyled>

<Unstyled>
  <Story of={Figures.OutOfScope} />
</Unstyled>

---

## Glossary

<DocsTable
  headers={['Term', 'Definition']}
  rows={[
    ['Token', 'A named design value, for example primary/600 or spacing/2.'],
    ['Primitive', 'A raw value with a neutral name: blue/500, gray/100. Not referenced directly by components.'],
    ['Semantic', 'A role that references a primitive: primary/600, surface/50, text/muted. Components reference semantic tokens.'],
    ['tokens.json', 'The Figma export stored in the repository. Input for every generated file.'],
    ['--pds-*', 'A Plectrum CSS custom property. The only token name written in component SCSS.'],
    ['--p-*', 'A PrimeNG theme custom property, emitted by the preset. Bridged to --pds-* in 01-settings only.'],
    ['Preset', 'The PrimeNG theme configuration. v1 runs in production; v0.6 remains available via the toolbar toggle.'],
    ['Staging branch', 'design-tokens/sync. The only branch the Figma plugin writes to. Promotion to main requires a reviewed pull request.'],
    ['Pull request (PR)', 'A proposed change reviewed before merge.'],
    ['CI', 'Automated checks on every change: audits, build, lint, pack smoke test.'],
    ['Package', 'A published, versioned artifact: @solidaris/ui, @solidaris/styles, @solidaris/plectrum, @solidaris/tokens-cli.'],
  ]}
/>

---

## Reference

### Commands

Scripts live in `tools/tokens/`. They are tools, not chores — by default an **agent** runs them while doing the task. Running them yourself is always possible and always a choice. CI runs them regardless.

<DocsTable
  headers={['Context', 'Trigger', 'Notes']}
  rows={[
    ['Agent — default', 'Any task that touches tokens: a token change, a component scaffold, an audit', 'The agent runs the scripts and reads the output. Ask for the change, not for the command.'],
    ['Manual', 'A terminal at the workspace root', 'Same commands, same output. Useful for spot checks and local debugging.'],
    ['CI', 'Every push and pull request', 'The blocking gate. Applies to agent and human changes alike.'],
  ]}
/>

Do not edit `*.generated.scss` or `tokens.generated.ts` by hand — `tokens:build` is the only writer. `tokens:resolve` is both a CLI and the shared resolver imported by audit, validate, build, propose and pull.

<DocsTable
  headers={['npm', 'Script', 'Role', 'CI']}
  rows={[
    ['tokens:resolve', 'resolve-dtcg.mjs', 'Flatten the seven DTCG sets in tokens.json and resolve {alias} chains. Detects cycles and missing refs.', 'Library for the other scripts. Not a CI step.'],
    ['tokens:audit', 'audit-drift.mjs', 'Three-way compare: tokens.json, Plectrum_v1 preset, and --pds-* in 01-settings, plus alias-map.json breaks. Reports only; does not rewrite values.', 'Blocking'],
    ['tokens:validate-preset', 'validate-preset.mjs', 'Every {token.path} in the imported Plectrum_v1 default export must resolve to a literal in tokens.json.', 'continue-on-error until tokens.json covers every PrimeNG path'],
    ['tokens:check-prefix', 'check-prefix.mjs', 'Reject new bare --spacing- / --text- / --font- / --line-height- declarations outside _settings.legacy-aliases.scss.', 'Blocking'],
    ['tokens:build', 'build.mjs', 'Style Dictionary writes hybrid --pds-* SCSS and the Storybook annotation manifest. Spacing and typography stay hand-authored.', 'Blocking — generated files must be committed'],
    ['tokens:lint', 'lint-usage.mjs', '--p-* declarations outside 01-settings, PrimeUI runtime imports in apps/ or libs/ui, unknown --pds-* names.', 'Blocking. --strict (hex/px) is local only'],
    ['tokens:propose', 'propose-to-figma.mjs', 'Diff code-owned --pds-* in 01-settings against tokens.json. Emit proposed.dtcg.json.', 'Not in default CI. Used by apply-to-figma.yml'],
    ['tokens:apply', 'apply-to-figma.mjs', 'POST proposed variables to the Figma branch proposals/{app}. Dry-run is the default. Aborts if the branch is missing. Never writes the main file.', 'Manual / workflow_dispatch with Environment approval'],
    ['tokens:pull-figma', 'pull-figma.mjs', 'Read-only Variables API check. Flags published Figma variables that never landed in tokens.json.', 'library-publish.yml safety net'],
    ['pack:smoke', 'tools/packaging/smoke-test.mjs', 'Pack library tarballs, install them in a throwaway Angular app without path aliases, and build it.', 'pack-smoke job'],
  ]}
/>

<DocsTable
  headers={['npm', 'Reads', 'Writes']}
  rows={[
    ['tokens:resolve', 'libs/plectrum/src/tokens.json', 'stdout or --out JSON. Nothing in the repo unless --out is set.'],
    ['tokens:audit', 'tokens.json, Plectrum_v1, 01-settings, tools/tokens/alias-map.json, tools/tokens/audit-allowlist.json', 'stdout report. Exit non-zero on drift.'],
    ['tokens:validate-preset', 'Plectrum_v1/ts default export, tokens.json', 'stdout. Exit non-zero when a preset path is missing or unresolved.'],
    ['tokens:check-prefix', 'workspace SCSS (skips node_modules, dist, storybook-static)', 'stdout. Exit non-zero on a new bare declaration.'],
    ['tokens:build', 'tokens.json, alias-map.json, tools/tokens catalog', 'libs/styles/src/01-settings/_settings.{colors-primitive,colors-semantic,radius,shadows,transitions,focus}.generated.scss and libs/ui/src/storybook/tokens.generated.ts'],
    ['tokens:lint', 'apps/, libs/ui, 01-settings, tokens.generated.ts', 'stdout. Exit non-zero on a hard failure.'],
    ['tokens:propose', '01-settings --pds-* decls, tokens.json', 'tools/tokens/proposed.dtcg.json (override with --out)'],
    ['tokens:apply', 'proposed.dtcg.json, FIGMA_TOKEN, Figma branch list', 'Figma branch only, and only with --write. Dry-run prints the POST payload.'],
    ['tokens:pull-figma', 'FIGMA_TOKEN, Figma Variables API, tokens.json', 'stdout. Exit 1 when a published variable has no tokens.json path.'],
  ]}
/>

<DocsTable
  headers={['Flag / env', 'Scripts', 'Effect']}
  rows={[
    ['--file path', 'tokens:resolve', 'Alternate tokens.json. Default: libs/plectrum/src/tokens.json.'],
    ['--out path', 'tokens:resolve, tokens:propose', 'Write JSON to a path instead of the default (resolve: stdout; propose: tools/tokens/proposed.dtcg.json).'],
    ['--quiet', 'tokens:resolve', 'Suppress the summary line.'],
    ['--strict', 'tokens:lint', 'Fail on hardcoded hex/px in apps/ and libs/ui. Without it those findings are informational.'],
    ['--app <name>', 'tokens:apply', 'Resolve Figma branch proposals/{name}. Required for a real apply.'],
    ['--dry-run', 'tokens:apply', 'Default. Print payload and branch key. Zero writes.'],
    ['--write', 'tokens:apply', 'POST /v1/files/:branchKey/variables. Needs FIGMA_TOKEN and Environment approval.'],
    ['--proposal path', 'tokens:apply', 'Alternate proposed.dtcg.json.'],
    ['--file-key KEY', 'tokens:apply, tokens:pull-figma', 'Override FIGMA_FILE_KEY. Default is the Plectrum UI Kit.'],
    ['FIGMA_TOKEN', 'tokens:apply, tokens:pull-figma', 'Repo secret. Unset: apply stays dry-run; pull skips with a message.'],
    ['FIGMA_FILE_KEY', 'tokens:apply, tokens:pull-figma', 'Optional file key. Same default as --file-key.'],
  ]}
/>

<DocsTable
  headers={['When', 'Run']}
  rows={[
    ['tokens.json or alias-map.json changed', 'tokens:audit, then tokens:build. Commit the generated SCSS and tokens.generated.ts.'],
    ['A --pds-* declaration was added in 01-settings', 'tokens:check-prefix, tokens:lint, tokens:propose if it should exist in Figma.'],
    ['Component SCSS uses a new --pds-* name', 'tokens:lint. Add the name to the component .metadata.ts tokens.consumed list.'],
    ['Proposing a code-owned token to Figma', 'tokens:propose, then tokens:apply -- --app <name> (dry-run first, --write after review).'],
    ['Suspect the plugin missed a Figma publish', 'tokens:pull-figma (needs FIGMA_TOKEN).'],
    ['Checking a consumer install', 'pack:smoke. CI also builds Storybook against packed tarballs.'],
  ]}
/>

```bash
npm run tokens:resolve            # flatten DTCG + resolve {alias} (library + CLI)
npm run tokens:audit              # three-way Figma / v1 / SCSS + alias-map
npm run tokens:validate-preset    # every {token.path} in Plectrum_v1/ts
npm run tokens:check-prefix       # no new bare --spacing- / --text- / --font- decls
npm run tokens:build              # regenerate *.generated.scss + tokens.generated.ts
npm run tokens:lint               # --p-* decls, PrimeUI runtime imports, unknown --pds-*
npm run tokens:lint -- --strict   # also fail hardcoded hex/px
npm run tokens:propose            # code-only tokens → proposed.dtcg.json
npm run tokens:apply -- --app scratch --dry-run
npm run tokens:apply -- --app scratch --write
npm run tokens:pull-figma         # Variables API safety net (FIGMA_TOKEN)
npm run pack:smoke                # pack tarballs and build a throwaway app
```

CI (`.github/workflows/ci.yml`) runs audit, prefix guard, `tokens:build` plus a **scoped** `git diff --exit-code` on `*.generated.scss` and `tokens.generated.ts` only, then lint. A separate `pack-smoke` job runs `pack:smoke`. `storybook-packed` builds Storybook against the packed tarballs. `tokens:validate-preset` is `continue-on-error` until `tokens.json` covers every PrimeNG path. The build diff does not fail on unrelated lockfile or workspace noise.

### Delivery status

<DocsTable
  headers={['Wave', 'Scope', 'Status']}
  rows={[
    ['1', 'Drift detector (resolve-dtcg, audit-drift, validate-preset) before any value rewrite', 'Done — audit is blocking'],
    ['2', 'Prefix migration + legacy aliases last + tokens:check-prefix', 'Done'],
    ['3', 'alias-map.json, hybrid Style Dictionary, *.generated.scss', 'Done — spacing/type stay hand-authored'],
    ['4', 'tokens.generated.ts, token explorer, foundation stories, lint, docs hygiene', 'Done'],
    ['5', 'Staging tree, tokens-sync.yml, plugin setup, pull-figma safety net', 'Done'],
    ['6', 'Publishable @solidaris/ui / plectrum / styles, changesets, pack smoke test', 'Done'],
    ['7', 'propose-to-figma + apply-to-figma (dry-run default) + LIBRARY_PUBLISH', 'Done'],
    ['8', 'Renovate/Dependabot templates, token-usage CLI, component promotion', 'Done'],
  ]}
/>
