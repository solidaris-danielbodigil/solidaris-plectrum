import { Meta } from '@storybook/addon-docs/blocks';
import { DocsTable } from '../../.storybook/docs-table';

<Meta title="Docs/Troubleshooting" />

# Troubleshooting

Common Storybook and token-pipeline failures, and what to do.

<DocsTable
  headers={['Symptom', 'Cause', 'Fix']}
  rows={[
    ['`--p-*` tokens are empty / PrimeNG looks unthemed', '`providePlectrum()` has not booted yet', 'Call `providePlectrum()` (or `providePlectrum(\'v0.6\')`) in the app `applicationConfig`. Storybook already does this in `libs/ui/.storybook/preview.ts`.'],
    ['Storybook will not start after `npm run storybook:packed`', 'Packed mode resolves `@solidaris/*` from tarballs, not the workspace', 'Use `npm run storybook` for day-to-day work. Packed Storybook is a release smoke, not the default loop.'],
    ['Windows: SCSS or Storybook watcher never picks up a save', 'Native `fs.watch` misses some NTFS events', 'Restart the process. If it persists, run the command again from WSL or raise an issue with the file path that stalled.'],
    ['CI: `tokens:build` diff fails', 'Generated SCSS or `tokens.generated.ts` is stale', 'Run `npm run tokens:build` and commit `libs/styles/src/01-settings/*.generated.scss` plus `libs/ui/src/storybook/tokens.generated.ts`.'],
    ['CI: `generate-index` diff fails', '`.ai/contracts/index.json` is stale after a component or metadata edit', 'Run `npm run generate-index` and commit the index. Do not edit `index.json` by hand.'],
    ['CI: changelog feed diff fails', '`changelog.generated.ts` is stale after a changeset or Version PR', 'Run `npm run changelog:build` and commit the file. The Release workflow runs this after `changeset version` (`npm run changeset:version`).'],
    ['Dialog / Drawer / Popover / Menu clipped or behind the shell', 'Overlay rendered inside a stacking or overflow context', 'Set `appendTo="body"` (or `pdsOverlayAppendTo()`). The PrimeNG gallery Overlays page shows the endorsed pattern.'],
    ['A `*-stories.mdx` page never appears in the sidebar', 'Storybook 10 only attaches MDX as `name.mdx` next to `name.stories.ts`', 'Name the prose file `{name}.mdx`, not `{name}.stories.mdx`. Docs-only pages use `<Meta title="Docs/…" />`.'],
    ['`test-storybook` fails on accessibility', '`parameters.a11y.test` is `error` and addon-a11y found a WCAG 2.1 AA violation', 'Fix the story (missing name, contrast, focus). Do not flip the gate back to `todo`.'],
    ['Stock “Level up” onboarding widget still shows', 'Storybook 10 may ignore `enableOnboarding: false` on some manager builds', 'Leave it. `libs/ui/.storybook/manager.ts` already sets the flag when the manager honours it.'],
  ]}
/>
