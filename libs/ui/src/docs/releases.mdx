import { Meta } from '@storybook/addon-docs/blocks';
import { DocsTable } from '../../.storybook/docs-table';

<Meta title="Docs/Releases and versioning" />

# Releases and versioning

Changesets versions the three runtime packages together. Applications install the current packed tarballs until a verified GitHub Packages release exists. Package manifests and What's new describe source versions, not registry publication.

Related pages:

- [Get started / Use Plectrum in an app](?path=/docs/get-started-use-plectrum-in-an-app--docs) — the consumer side
- [Token pipeline](?path=/docs/docs-token-pipeline--docs) — how token changes reach a release

---

## Process

1. A change lands with a **changeset** (`npm run changeset`) describing the bump (patch / minor / major) and the affected packages.
2. On merge to `main`, the release workflow (`.github/workflows/release.yml`, `changesets/action`) opens or updates the **version PR** — `chore(release): version packages` — accumulating pending changesets into version bumps and changelogs.
3. CI packs the four artifacts, builds an isolated consumer and checks package ownership, registry target and tarball integrity with `npm run release:check`. Registry publication and immutable documentation are still pending P7.
4. Application repositories receive the bump as a Renovate / Dependabot pull request (`tools/consumers/renovate.json`).

## Rules

<DocsTable
  headers={['Rule', 'Detail']}
  rows={[
    ['Fixed version group', '@solidaris-danielbodigil/ui, @solidaris-danielbodigil/plectrum and @solidaris-danielbodigil/styles always version together (.changeset/config.json → fixed)'],
    ['Toolkit version', '@solidaris-danielbodigil/plectrum-devkit has its own version and declares compatible runtime and process ranges; its snapshot must match installed runtime packages'],
    ['Internal token scripts', '@solidaris/tokens-cli is a private workspace maintenance package; consumer token checks ship in the devkit'],
    ['Changeset per change', 'A PR that alters published behaviour carries a changeset; docs-only and Storybook-only changes do not'],
    ['Commit gate', 'npm run check:commit runs before the commit is created. git commit --no-verify skips it. The pull request into main still runs the full CI gates'],
    ['CI gates before release', 'Token audits, generated-file diffs, contracts-index freshness, build, tests and the pack smoke test all pass on main first'],
    ['Registry and ownership', 'The four manifests and the token-free repository .npmrc route @solidaris-danielbodigil to private GitHub Packages. Consumer repositories need package read access.'],
    ['Consumer proof', 'pack:smoke installs the packed tarballs into a throwaway app without path aliases and builds it; storybook-packed builds this catalogue against the same tarballs'],
  ]}
/>

## What ships

<DocsTable
  headers={['Package', 'Contents']}
  rows={[
    ['@solidaris-danielbodigil/ui', 'Angular components (Angular Package Format)'],
    ['@solidaris-danielbodigil/plectrum', 'providePlectrum(), presets, tokens.json'],
    ['@solidaris-danielbodigil/styles', 'ITCSS SCSS source (apps add it to includePaths)'],
    ['@solidaris-danielbodigil/plectrum-devkit', 'Versioned metadata, schemas, process, token inventory and team checks'],
  ]}
/>

This Storybook currently documents a development preview of `main`; package versions in its tables do not prove registry publication. The packed-Storybook CI job verifies local tarballs. An immutable Storybook per published version belongs to the release pipeline.
