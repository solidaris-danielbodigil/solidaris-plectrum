import { Meta } from '@storybook/addon-docs/blocks';
import { DocsTable } from '../../.storybook/docs-table';
import contracts from '../../../../.ai/contracts/index.json';

<Meta title="Docs/Component status" />

# Component status

Living index from `.ai/contracts/index.json` (regenerated by `npm run generate-index`, CI-gated). CSS-only blocks stay on their own pages — they are not Angular components and do not appear here.

<DocsTable
  headers={['Name', 'Status', 'Owner', 'Category', 'PrimeNG base', 'Modified']}
  rows={Object.entries(contracts.components).map(([name, entry]) => [
    name,
    entry.status ?? 'undeclared',
    entry.owner ?? '—',
    entry.category,
    entry.primeNg ?? '—',
    contracts.meta.generated,
  ])}
/>
