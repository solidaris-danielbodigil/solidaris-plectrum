import { Meta, Story, Unstyled } from '@storybook/addon-docs/blocks';
import * as Figures from './whats-new.stories';

<Meta title="Docs/What's new" />

# What's new

What the next version PR publishes, and what the published versions changed. Generated from pending `.changeset/*.md` and `libs/*/CHANGELOG.md` by `npm run changelog:build`; the file `libs/ui/src/storybook/changelog.generated.ts` is CI-gated — do not edit it by hand. Bumps follow one colour scale: major, minor, patch. Process and rules: [Releases and versioning](?path=/docs/docs-releases-and-versioning--docs).

## Unreleased

Pending changesets on `main`. The three packages version together, so the header shows the bump the next version PR applies to each of them.

<Unstyled>
  <Story of={Figures.Unreleased} />
</Unstyled>

## Released

Published versions, newest first. A change shared by several packages appears once.

<Unstyled>
  <Story of={Figures.Released} />
</Unstyled>
