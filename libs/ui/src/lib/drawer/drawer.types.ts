/** Edge a PrimeNG drawer slides in from. */
export type DrawerPosition = 'left' | 'right' | 'top' | 'bottom';

/** Label / value row for `c-detail-list` description lists. */
export interface DetailListRow {
  label: string;
  value: string;
}
