import { Meta, Canvas, Controls, Story, Unstyled } from '@storybook/addon-docs/blocks';
import * as Stories from './list.stories';

<Meta of={Stories} />

# List

<Unstyled>
  <Story of={Stories.Status} />
</Unstyled>

## Usage

<Unstyled>
  <Story of={Stories.Usage} />
</Unstyled>

## Anatomy

<Unstyled>
  <Story of={Stories.Anatomy} />
</Unstyled>

## Default

Default journey (grouped) list for Eva Martinez — Parcours Indemnités groups with timeline gutter and selected document row.

<Canvas of={Stories.Default} />
<Controls of={Stories.Default} />

## Grouped

Alias of Default — journey ON with expandable groups, chevrons, and timeline gutters.

<Canvas of={Stories.Grouped} />

## Flat

Flat mode (journey OFF) — full-width document cards without chevron or timeline.

<Canvas of={Stories.Flat} />

## Group expanded

All journey groups expanded — child document rows visible beneath each group header.

<Canvas of={Stories.GroupExpanded} />

## Group collapsed

First Parcours Indemnités group collapsed — chevron rotated, child document rows hidden.

<Canvas of={Stories.GroupCollapsed} />

## Selected document

Selected document row — 1px primary border and `shadow-md` elevation.

<Canvas of={Stories.SelectedDocument} />

## Row default

Default document row via `pds-list` — no selection.

<Canvas of={Stories.RowDefault} />

## Row hover

Hover document row via `pds-list` — static capture uses `.sb-list-row--hover`.

<Canvas of={Stories.RowHover} />

## Row selected

Selected document row via `pds-list` and `selectedItemId`.

<Canvas of={Stories.RowSelected} />

## Loading

Static loading state — PrimeNG `p-skeleton` placeholder rows.

<Canvas of={Stories.Loading} />

## Simulated loading

Flashes skeleton for 1000ms then reveals content — use to preview the shimmer transition.

<Canvas of={Stories.SimulatedLoading} />

## Row states

Flat document row chrome — default (neutral border), hover (primary border, no shadow), and selected (primary border + `shadow-md`). Hover column uses `.sb-force-hover` for static docs capture. Group headers do not receive hover/selected chrome.

<Canvas of={Stories.RowStates} />

## Composition

<Unstyled>
  <Story of={Stories.Composition} />
</Unstyled>

## Behavior

<Unstyled>
  <Story of={Stories.Behavior} />
</Unstyled>

## Accessibility

<Unstyled>
  <Story of={Stories.Accessibility} />
</Unstyled>

