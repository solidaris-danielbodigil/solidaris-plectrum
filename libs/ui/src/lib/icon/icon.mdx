import { Meta, Canvas, Controls, ArgTypes, Story, Unstyled } from '@storybook/addon-docs/blocks';
import { DocsTable } from '../../../.storybook/docs-table';
import * as Stories from './icon.stories';

<Meta of={Stories} />

# Icon

<Unstyled>
  <Story of={Stories.Status} />
</Unstyled>

Universal icon primitive for the Plectrum Design System. Renders a Bootstrap Icons class or a custom SVG from `IconRegistry`.

## When to use

- Navigation item icons
- Button leading or trailing icons
- Status and feedback indicators
- Standalone labelled icons in empty states

## When not to use

- Setting an explicit colour on `pds-icon` — inherit `currentColor` from the parent
- Omitting `label` when the icon is the only indicator of meaning
- `source="svg"` without registering the key first — the icon renders nothing

## Anatomy

<DocsTable
  headers={['Part', 'Role']}
  rows={[
    ['c-icon', 'Host — size modifier maps to --pds-icon-size-*'],
    ['source="class"', 'Bootstrap Icons class on an inner i (default)'],
    ['source="svg"', 'Inline SVG from IconRegistry'],
    ['label', 'When set: role="img" + aria-label; when omitted: aria-hidden'],
  ]}
/>

## Accessibility

- Decorative (no `label`): `aria-hidden="true"` — the surrounding control carries the name
- Standalone: pass `label` so the host gets `role="img"` and `aria-label`
- The inner Bootstrap `<i>` is always `aria-hidden`

## Figma

[Icons — Plectrum UI Kit](https://www.figma.com/design/YNZ1DlSjDNUXrvkxlSp10D/Plectrum-for-PrimeNG--Main-)

The searchable catalog lives under [Foundations / Iconography](?path=/docs/foundations-iconography--docs). This page documents the `pds-icon` component only.

- **`source="class"`** (default) — pass any Bootstrap Icons class string, e.g. `bi bi-house`
- **`source="svg"`** — pass a registry key previously registered via `IconRegistry.register(name, svg)`
- **`size`** — `xs | sm | md | lg | xl` — maps to `--pds-icon-size-*` tokens

## Default

Default Bootstrap Icons class icon at `md` size, decorative (`aria-hidden`).

<Canvas of={Stories.Default} />
<Controls of={Stories.Default} />

## Size variants

All five size tokens: `xs`, `sm`, `md`, `lg`, `xl`.

<Canvas of={Stories.SizeVariants} />

## Standalone accessible

Non-decorative icon with `label` — gets `role="img"` and `aria-label`.

<Canvas of={Stories.StandaloneAccessible} />

## Custom SVG from registry

Custom SVG registered via `IconRegistry.register()` and rendered via `source="svg"`.

<Canvas of={Stories.CustomSvgFromRegistry} />

## Colour inheritance

Icons inherit `currentColor` from the surrounding text colour.

<Canvas of={Stories.ColourInheritance} />

## API

<ArgTypes of={Stories} />
