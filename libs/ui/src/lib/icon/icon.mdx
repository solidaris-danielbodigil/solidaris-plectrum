import { Meta, Canvas, Controls, Story, Unstyled } from '@storybook/addon-docs/blocks';
import * as Stories from './icon.stories';

<Meta of={Stories} />

# Icon

<Unstyled>
  <Story of={Stories.Status} />
</Unstyled>

## Usage

<Unstyled>
  <Story of={Stories.Usage} />
</Unstyled>

## Anatomy

<Unstyled>
  <Story of={Stories.Anatomy} />
</Unstyled>

## Variants

<Unstyled>
  <Story of={Stories.Variants} />
</Unstyled>

## Default

Default Bootstrap Icons class icon at `md` size, decorative (`aria-hidden`). Browse the searchable glyph catalog under [Foundations / Iconography](?path=/docs/foundations-iconography--docs) — this page documents the `pds-icon` component only.

<Canvas of={Stories.Default} />
<Controls of={Stories.Default} />

## Size variants

All five size tokens: `xs`, `sm`, `md`, `lg`, `xl`.

<Canvas of={Stories.SizeVariants} />

## Standalone accessible

Non-decorative icon with `label` — gets `role="img"` and `aria-label`.

<Canvas of={Stories.StandaloneAccessible} />

## Custom SVG from registry

Custom SVG registered via `IconRegistry.register()` and rendered via `source="svg"`.

<Canvas of={Stories.CustomSvgFromRegistry} />

## Colour inheritance

Icons inherit `currentColor` from the surrounding text colour.

<Canvas of={Stories.ColourInheritance} />

## Behavior

<Unstyled>
  <Story of={Stories.Behavior} />
</Unstyled>

## Accessibility

<Unstyled>
  <Story of={Stories.Accessibility} />
</Unstyled>

