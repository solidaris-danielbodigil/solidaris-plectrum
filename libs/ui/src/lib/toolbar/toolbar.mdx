import { Meta, Canvas, Controls, Story, Unstyled } from '@storybook/addon-docs/blocks';
import * as Stories from './toolbar.stories';

<Meta of={Stories} />

# Toolbar

<Unstyled>
  <Story of={Stories.Status} />
</Unstyled>

## Usage

<Unstyled>
  <Story of={Stories.Usage} />
</Unstyled>

## Default

<Canvas of={Stories.Default} />
<Controls of={Stories.Default} />

## Anatomy

<Unstyled>
  <Story of={Stories.Anatomy} />
</Unstyled>

## Sticky

`position: sticky` sits on the host element, so it works against the nearest scroll container — including overflow-constrained app shells.

<Canvas of={Stories.Sticky} />

## Bordered

`variant="bordered"` uses the existing `u-border-all` + `--pds-color-card-border` ring on the inner PrimeNG Card and clears `--p-card-shadow`.

<Canvas of={Stories.Bordered} />

## Composition

<Unstyled>
  <Story of={Stories.Composition} />
</Unstyled>

### Start slot only

The end wrapper stays empty when nothing is projected into `[slot=end]`.

<Canvas of={Stories.StartSlotOnly} />

## Behavior

<Unstyled>
  <Story of={Stories.Behavior} />
</Unstyled>

## Accessibility

<Unstyled>
  <Story of={Stories.Accessibility} />
</Unstyled>

