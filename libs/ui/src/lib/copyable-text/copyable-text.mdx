import { Meta, Canvas, Controls, Story, Unstyled } from '@storybook/addon-docs/blocks';
import * as Stories from './copyable-text.stories';

<Meta of={Stories} />

# Copyable Text

<Unstyled>
  <Story of={Stories.Status} />
</Unstyled>

## Usage

<Unstyled>
  <Story of={Stories.Usage} />
</Unstyled>

## Anatomy

<Unstyled>
  <Story of={Stories.Anatomy} />
</Unstyled>

## Default

Single identifier chip. Click it to copy the value — these canvases listen to `(copied)` and show a confirmation toast on the Storybook preview frame (`Copié !`); the chip itself does not own toast UI.

<Canvas of={Stories.Default} />
<Controls of={Stories.Default} />

## Identifier row

Multiple identifiers in a row with bullet separators — same layout as the profile overview card. Each chip shows the copy toast.

<Canvas of={Stories.IdentifierRow} />

## Custom aria-label

Override the default `Copier {label}` accessible name.

<Canvas of={Stories.CustomAriaLabel} />

## Disabled

Chip is not interactive and does not write to the clipboard.

<Canvas of={Stories.Disabled} />

## Composition

<Unstyled>
  <Story of={Stories.Composition} />
</Unstyled>

## Behavior

<Unstyled>
  <Story of={Stories.Behavior} />
</Unstyled>

## Accessibility

<Unstyled>
  <Story of={Stories.Accessibility} />
</Unstyled>

