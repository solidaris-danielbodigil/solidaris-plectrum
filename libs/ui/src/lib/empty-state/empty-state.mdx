import { Meta, Canvas, Controls, ArgTypes, Story, Unstyled } from '@storybook/addon-docs/blocks';
import { DocsTable } from '../../../.storybook/docs-table';
import * as Stories from './empty-state.stories';

<Meta of={Stories} />

# Empty State

<Unstyled>
  <Story of={Stories.Status} />
</Unstyled>

Reusable placeholder for empty results, document details, or onboarding states.

## When to use

- Empty result panes
- Placeholder document details
- Onboarding empty states with a short heading and one supporting sentence

## When not to use

- Actionable error recovery — this block is descriptive, not interactive. Use a dedicated error banner or recovery screen

## Anatomy

<DocsTable
  headers={['Part', 'Role']}
  rows={[
    ['c-empty-state', 'Section landmark for the placeholder'],
    ['c-empty-state__art', 'Decorative illustration frame'],
    ['c-empty-state__illustration', 'Catalog SVG (img)'],
    ['c-empty-state__title', 'Visible heading that carries the message'],
    ['c-empty-state__description', 'Optional supporting sentence'],
  ]}
/>

## Accessibility

- The hero art is decorative — the title and description carry the message
- Screen readers ignore the SVG (`aria-hidden` on the art frame, empty `alt` on the image)

## Figma

[Empty state illustration](https://www.figma.com/design/YNZ1DlSjDNUXrvkxlSp10D/Plectrum-for-PrimeNG--Main-?node-id=441-7641)

## Illustrations

`illustration` defaults to **`random`**: one catalog SVG is chosen when the instance is created, and that pick stays for the lifetime of the component.

Pass an illustration id to pin a specific asset — useful in Storybook, visual tests, or a screen that should always show the same drawing.

```html
<pds-empty-state
  title="Aucun résultat"
  description="Lancez une recherche."
/>

<pds-empty-state
  title="Aucun résultat"
  description="Lancez une recherche."
  illustration="people-search"
/>
```

## Default

Random illustration. Use **Autre illustration** to pick another catalog SVG, or pin one with the `illustration` control.

<Canvas of={Stories.Default} />
<Controls of={Stories.Default} />

## All illustrations

Every catalog SVG, each pinned with its id.

<Canvas of={Stories.AllIllustrations} />

## API

<ArgTypes of={Stories} />
