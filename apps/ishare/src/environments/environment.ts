/**
 * iSHARE runtime configuration.
 * `enableTestingTelemetry` stays false in production builds — use the
 * development file replacement for moderated user-testing sessions.
 */
export const environment = {
  production: true,
  enableTestingTelemetry: false,
};
